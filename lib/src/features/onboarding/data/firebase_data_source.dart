import 'package:octattoo_app/src/features/onboarding/domain/user_onboarding_info.dart';

class FirebaseDataSource {
  Future<void> submitOnboardingInfoToFirebase(UserOnboardingInfo info) async {
    // Implement your Firebase submission logic here.
    // Example:
    // final ref = FirebaseFirestore.instance.collection('users').doc();
    // await ref.set(info.toJson());
  }
}
