import 'package:flutter/material.dart';

class Step2AddWorkplace extends StatelessWidget {
  const Step2AddWorkplace({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
