enum TopLevelDestinations {
  welcome,
  onboarding,
  dashboard,
}

enum WelcomeSubRoutes {
  signin,
  register,
}

enum SignInSubRoutes {
  forgotPassword,
}

enum OnboardingSubRoutes {
  artistProfile,
  workplace,
}

enum WorkplaceSubRoutes {
  add,
  details,
  availabilities,
}

enum DashboardSubRoutes {
  appointments,
  customers,
  projects,
  artistProfile,
  // quotations,
  // invoices,
  // payments,
  // traceability,
  // inventory,
  // settings,
}
